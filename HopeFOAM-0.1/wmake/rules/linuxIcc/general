CPP        = /lib/cpp -traditional-cpp $(GFLAGS)
LD         = ld -melf_i386

PROJECT_LIBS = -l$(ORIGIN_PROJECT) -ldl

include $(GENERAL_RULES)/standard

include $(DEFAULT_RULES)/c
include $(DEFAULT_RULES)/c++
